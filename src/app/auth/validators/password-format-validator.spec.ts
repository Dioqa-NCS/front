import { PasswordFormatValidator } from './password-format-validator';

describe('PasswordFormatValidator', () => {
  it('should create an instance', () => {
    expect(new PasswordFormatValidator()).toBeTruthy();
  });
});

import { NumberFormatValidator } from './number-format-validator';

describe('NumberFormatValidator', () => {
  it('should create an instance', () => {
    expect(new NumberFormatValidator()).toBeTruthy();
  });
});
